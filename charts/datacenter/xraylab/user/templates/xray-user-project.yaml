apiVersion: project.openshift.io/v1
kind: Project
metadata:
  name: "{{ .Values.xraylab.user }}"
  annotations:
    openshift.io/description: ""
    openshift.io/display-name: "{{ .Values.xraylab.user }}"
