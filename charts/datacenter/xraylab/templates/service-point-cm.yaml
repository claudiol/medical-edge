apiVersion: v1
kind: ConfigMap
metadata:
  name: service-point
  namespace: "{{ .Values.global.xraylab.namespace }}"
data:
  url: "{{ .Values.global.ocs-storage.serviceUrl }}" # No trailing /
  url-external: "{{ .Values.global.ocs-storage.externalUrl }}" # No trailing /
