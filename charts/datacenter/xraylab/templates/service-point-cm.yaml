apiVersion: v1
kind: ConfigMap
metadata:
  name: service-point
  namespace: "{{ .Values.global.xraylab.namespace }}"
data:
  url: "{{ .Values.global.ocsstorage.serviceUrl }}" # No trailing /
  url-external: "{{ .Values.global.ocsstorage.externalUrl }}" # No trailing /
